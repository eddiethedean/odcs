version: "3.1.0"
apiVersion: "v3.1.0"
kind: "DataContract"
id: "invalid-relationship-dangling"
status: "active"

schema:
  - name: "orders"
    logicalType: "object"
    properties:
      - name: "customer_id"
        logicalType: "string"
    relationships:
      - from: "orders.customer_id"
        to: "ghost_table.ghost_col"

version: "3.1.0"
apiVersion: "v3.1.0"
kind: "DataContract"
id: "pricing-contract"
name: "pricing_contract"
status: "active"

price:
  priceAmount: 100.0
  priceCurrency: "USD"
  priceUnit: "per TB"

schema:
  - name: "customers"
    logicalType: "object"
    properties:
      - name: "customer_id"
        logicalType: "string"

version: "3.1.0"
apiVersion: "v3.1.0"
kind: "DataContract"
id: "invalid-json-schema-only"
status: "draft"

schema:
  - name: "customers"
    logicalType: "object"
    properties:
      - name: "customer_id"
        logicalType: "string"
    quality:
      - name: "bad-dimension"
        type: "text"
        dimension: "not-a-valid-dimension"

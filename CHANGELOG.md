# Changelog

## 0.1.0

Phase 1 — Skeleton.

- Professional repository layout aligned with DTCS conventions
- Rust crate skeleton with module boundaries
- Basic YAML parsing for minimal ODCS contracts
- CLI with `version` and `validate` commands
- CI pipeline (fmt, clippy, test)
- Documentation reorganized under `docs/implementation/`
